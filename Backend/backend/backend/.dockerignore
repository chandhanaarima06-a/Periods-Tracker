# Build context exclusions — keeps the image small AND secret-free.

# Local build artifacts (never ship them)
target/
maven/

# application.properties IS included — it only holds env-var placeholders,
# no secrets. Real credentials come from environment variables at runtime.

# Wrapper scratch + logs + git history
*.working
*.log
.git
